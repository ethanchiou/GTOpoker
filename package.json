{
  "name": "gto-trainer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.28.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "depcruise packages --config .dependency-cruiser.cjs",
    "check": "pnpm run build && pnpm run lint && pnpm run test",
    "build:solver": "cd packages/solver-worker && wasm-pack build --target web --release --out-dir pkg && mkdir -p ../../apps/web/public/solver && cp pkg/gto_solver_wasm.js pkg/gto_solver_wasm_bg.wasm ../../apps/web/public/solver/",
    "build:solver:node": "cd packages/solver-worker && wasm-pack build --target nodejs --release --out-dir pkg-node",
    "validate:solver:reference": "cd packages/solver-worker/reference && cargo run --release > ../validation/reference-spots.json"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "dependency-cruiser": "^16.8.0",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "vite-tsconfig-paths": "^5.1.4",
    "vitest": "^2.1.8"
  }
}
